.content-box
  %h2 #{@contact.try(:name)} / #{@location.name}
= form_for [:admin, @location, @contact], html: { class: 'edit-entry' } do |f|
  = render 'admin/contacts/form', f: f, contact: @contact

