.inst-box
  %header
    = f.label :rank
    %span.desc
      = t('.description')

  = field_set_tag do
    .row
      .col-sm-6
        = f.select :rank, [1, 2, 3, 4, 5, ['No Rank', -1]], {}, class: 'form-control'
