%div.content-box
  %h1 Create a new location

= form_for [:admin, @location], url: admin_locations_path, html: { method: :post, class: 'edit_entry' } do |f|
  = render 'admin/locations/forms/new_location_form', f: f
