.inst-box
  %header
    %strong
      = f.label :keywords
    %p.desc
      = t('.description')

  = field_set_tag do
    = text_field_tag 'service[keywords][]', service.keywords.join(','), id: 'service_keywords', class: 'form-control'
